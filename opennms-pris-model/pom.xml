<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.opennms</groupId>
        <artifactId>opennms-pris-parent</artifactId>
        <version>3.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>opennms-pris-model</artifactId>

    <packaging>jar</packaging>

    <name>OpenNMS :: Provisioning Integration Server :: Model</name>

    <dependencies>
        <dependency>
            <groupId>org.jvnet.jaxb</groupId>
            <artifactId>jaxb-plugins-runtime</artifactId>
            <version>${jaxbVersion}</version>
        </dependency>
        <dependency>
            <groupId>jakarta.xml.bind</groupId>
            <artifactId>jakarta.xml.bind-api</artifactId>
            <version>${jakartaXmlBindApiVersion}</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jaxb</groupId>
            <artifactId>jaxb-core</artifactId>
            <version>${glassfishJaxbVersion}</version>
        </dependency>
        <!-- jaxb-plugins-runtime declares jaxb-runtime as provided (non-transitive),
             so pull the JAXB provider in explicitly for runtime and shading. -->
        <dependency>
            <groupId>org.glassfish.jaxb</groupId>
            <artifactId>jaxb-runtime</artifactId>
            <version>${glassfishJaxbVersion}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.eclipse.angus</groupId>
            <artifactId>angus-activation</artifactId>
            <version>${angusActivationVersion}</version>
            <scope>runtime</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <!-- Copy the XSDs to the project -->
            <plugin>
                <groupId>com.googlecode.maven-download-plugin</groupId>
                <artifactId>download-maven-plugin</artifactId>
                <version>${downloadMavenPluginVersion}</version>
                <executions>
                    <execution>
                        <id>unpack</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>artifact</goal>
                        </goals>
                        <configuration>
                            <groupId>org.opennms</groupId>
                            <artifactId>opennms-provision-persistence</artifactId>
                            <version>${opennmsVersion}</version>
                            <classifier>xsds</classifier>
                            <unpack>true</unpack>
                            <outputDirectory>target</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Compile XSDs into classes -->
            <plugin>
                <groupId>org.jvnet.jaxb</groupId>
                <artifactId>jaxb-maven-plugin</artifactId>
                <version>${jaxbVersion}</version>
                <executions>
                    <execution>
                        <id>xjc</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <!--
                    <verbose>true</verbose>
                    <debug>true</debug>
                    -->

                    <!-- Configure generated code -->
                    <enableIntrospection>true</enableIntrospection>

                    <!-- Specify XSDs -->
                    <schemaDirectory>${basedir}/target/xsds</schemaDirectory>
                    <schemaIncludes>
                        <include>model-import.xsd</include>
                    </schemaIncludes>

                    <!-- Specify bindings -->
                    <bindingDirectory>${basedir}/src/main/xjb</bindingDirectory>

                    <args>
                        <arg>-XtoString</arg>
                        <arg>-Xequals</arg>
                        <arg>-XhashCode</arg>
                        <arg>-Xfluent-api</arg>
                        <arg>-Xvalue-constructor</arg>
                    </args>

                    <plugins>
                        <!-- jaxb-plugins 4.x bundles the former jaxb2-basics,
                             jaxb2-fluent-api and jaxb2-value-constructor XJC
                             plugins, providing -XtoString/-Xequals/-XhashCode,
                             -Xfluent-api and -Xvalue-constructor. -->
                        <plugin>
                            <groupId>org.jvnet.jaxb</groupId>
                            <artifactId>jaxb-plugins</artifactId>
                            <version>${jaxbVersion}</version>
                        </plugin>
                    </plugins>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
