# Contribution workflow

- All pull requests MUST be created against `no42-org/opennms-provisioning-integration-server` (the `origin` fork) as the base repository — never against upstream `OpenNMS/opennms-provisioning-integration-server`.
- Always pass the base repository explicitly when creating pull requests: `gh pr create --repo no42-org/opennms-provisioning-integration-server`.
